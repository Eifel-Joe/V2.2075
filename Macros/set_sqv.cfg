[gcode_macro _SET_SQV]
gcode:
  {% set def = printer['toolhead'].square_corner_velocity|float %}
  {% set v = params.V|float %}
  {% if not params.V %}
    SET_VELOCITY_LIMIT SQUARE_CORNER_VELOCITY={def}
  {% else %}
    SET_VELOCITY_LIMIT SQUARE_CORNER_VELOCITY={v}
  {% endif %}